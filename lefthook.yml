pre-commit:
  parallel: true
  commands:
    svelte-check:
      glob: '*.{svelte,ts}'
      run: bunx svelte-check --tsconfig ./apps/app/tsconfig.json
    cargo-check:
      glob: '*.rs'
      run: cargo check --workspace
