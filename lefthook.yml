pre-commit:
  parallel: true
  commands:
    svelte-check:
      glob: '*.{svelte,ts}'
      run: bunx svelte-check --tsconfig ./apps/dowry/tsconfig.json
    cargo-check:
      glob: '*.rs'
      root: apps/dowry/src-tauri
      run: cargo check
