pre-commit:
  parallel: true
  commands:
    svelte-check:
      glob: '*.{svelte,ts}'
      run: bunx svelte-check --tsconfig ./apps/tauri/tsconfig.json
    cargo-check:
      glob: '*.rs'
      root: apps/tauri/src-tauri
      run: cargo check
